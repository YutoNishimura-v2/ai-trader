# Iter282 — stand-down range only — **FALSIFIED** OOS (2026-05-22): no-op vs iter274 on May.
extends: iter274_wavez_volgate_tiered.yaml

strategy:
  params:
    chop_vol_stand_down_enabled: true
    chop_vol_stand_down_regimes: [range]
