extends: ../default.yaml

# GOLD iter32: cap=0, 0/4, Mar -14.83%, Apr -9.43%, full -34.4% (vs iter179).

# Iter183 — Wave R: `zigzag_fib_mtf` — **1% risk** (vs Wave Q 2%) to reduce
# cap pressure while keeping the same signal recipe.

risk:
  risk_per_trade_pct: 1.0

instrument:
  timeframe: M1

strategy:
  __replace__: true
  name: zigzag_fib_mtf
  params:
    __replace__: true
    htf: M5
    zigzag_threshold_atr: 1.2
    zigzag_atr_period: 14
    atr_period_m1: 14
    fib_touch_atr: 0.45
    sl_atr_buffer: 0.35
    tp1_rr: 1.0
    tp2_rr: 2.5
    leg1_weight: 0.55
    cooldown_bars: 8
    session: always
    require_m1_bias_match: true
