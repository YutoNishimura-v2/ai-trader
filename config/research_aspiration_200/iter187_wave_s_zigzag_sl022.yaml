extends: ../default.yaml

# GOLD iter32: cap=1, 0/4, full -40.4%.

# Iter187 — Wave S: `zigzag_fib_mtf` — **tighter SL buffer** (`sl_atr_buffer: 0.22`)
# vs Wave Q baseline 0.35.

risk:
  risk_per_trade_pct: 2.0

instrument:
  timeframe: M1

strategy:
  __replace__: true
  name: zigzag_fib_mtf
  params:
    __replace__: true
    htf: M5
    zigzag_threshold_atr: 1.2
    zigzag_atr_period: 14
    atr_period_m1: 14
    fib_touch_atr: 0.45
    sl_atr_buffer: 0.22
    tp1_rr: 1.0
    tp2_rr: 2.5
    leg1_weight: 0.55
    cooldown_bars: 8
    session: always
    require_m1_bias_match: true
