extends: ../default.yaml

# GOLD iter32: cap=0, 1/4, full -3.9%, WS ~2.36.

# Iter204 — Wave W: iter195 geometry but **`session: overlap`** (12–16 UTC only)
# vs full `london_or_ny`.

risk:
  risk_per_trade_pct: 2.0

instrument:
  timeframe: M1

strategy:
  __replace__: true
  name: zigzag_fib_mtf
  params:
    __replace__: true
    htf: M5
    zigzag_threshold_atr: 1.2
    zigzag_atr_period: 14
    atr_period_m1: 14
    fib_touch_atr: 0.38
    sl_atr_buffer: 0.35
    tp1_rr: 1.0
    tp2_rr: 2.5
    leg1_weight: 0.55
    cooldown_bars: 8
    session: overlap
    require_m1_bias_match: true
