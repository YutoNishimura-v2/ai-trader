extends: session_sweep_reclaim_london.yaml

# Allow up to 2 trades per day. Asian-sweep can fire BOTH directions
# (sweep below, then sweep above, both reclaim back inside) on a single
# day during chop. The original 1/day cap was conservative.

strategy:
  params:
    max_trades_per_day: 2
